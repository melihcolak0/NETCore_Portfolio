@using EntityLayer.Concrete
@model List<WriterMessage>
@{
    ViewData["Title"] = "Sendbox";
    Layout = "~/Views/Shared/_AdminLayout2.cshtml";
}
<div class="page-header">
    <h3 class="page-title"> @ViewBag.v1 </h3>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="@ViewBag.v4">@ViewBag.v2</a></li>
            <li class="breadcrumb-item active" aria-current="page">@ViewBag.v3</li>
        </ol>
    </nav>
</div>
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Gönderilen</th>
                            <th>Konu</th>
                            <th>Mesaj</th>
                            <th>Tarih</th>
                            <th>Sil</th>
                            <th>Mesajı Gör</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var x in Model)
                        {
                            <tr>
                                <td>@x.WriterMessageId</td>
                                <td>@x.ReceiverName</td>
                                <td>@x.Subject</td>
                                <td>@(string.IsNullOrEmpty(x.MessageContent) ? x.MessageContent : (x.MessageContent.Length <= 20 ? x.MessageContent : x.MessageContent.Substring(0, 20) + "..."))</td>
                                <td>@x.Date.ToString("dd MMM yyyy - HH:mm")</td>
                                <td><a href="/AdminMessage/DeleteMessage/@x.WriterMessageId?returnUrl=Sendbox" class="btn btn-outline-danger">Sil</a></td>
                                <td><a href="/AdminMessage/MessageDetails/@x.WriterMessageId" class="btn btn-outline-info">Mesajı Gör</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="/corona-free-dark-bootstrap-admin-template-master/template/assets/vendors/js/vendor.bundle.base.js"></script>